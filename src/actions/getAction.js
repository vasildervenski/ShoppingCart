import { GET_PRODUCT_COUNT_IN_BASKET } from "./types";

export const getProductCount = () => {
  return (dispatch) => {
    // console.log("Get product count in basket");
    // dispatch({
    //   type: GET_PRODUCT_COUNT_IN_BASKET
    // });
  };
};
